import { StyleSheet } from 'react-native';

const btnStyleAtom = StyleSheet.create({
  btnContainer: {
    backgroundColor: 'purple',
    marginVertical: 10,
    marginHorizontal: 50,
    paddingVertical: 20,
    paddingHorizontal: 10,
  },
});

export default btnStyleAtom;
